It is a simple project that will expose a rest service to operate on a cart.
The data will be stored on a mongo db.

To test it run using
docker-compose up
then move into Test folder and run TestScript.sh
